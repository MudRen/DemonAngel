名称：
	getuid - 传回一个物件的 uid〈user id，使用者识别名称〉。
语法：
	string getuid( object ob );

	字串 getuid( 物件 ob );
语法：
	本函式仅在编译时定义 PACKAGE_UIDS 才有效。

	传回一个物件的 uid。一个物件的 uid 由物件创造当时呼叫的 
	creator_file() 函式决定。
参考：
	seteuid, geteuid, export_uid, valid_seteuid, creator_file
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻译：
	spock@muds.net		2000.Aug.24.	v22 版
