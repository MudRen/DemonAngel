MudOS LPC 目前支援浮点数形态 (floating point type). 
只需要如以下般宣告变数:

float pi;

一般来说, 整数的运算方式亦可用于浮点数. 浮点数常数 (constant) 可以包含小
数点, 例如:

pi = 3.14159265;

LPC 的浮点数形态, 基本上是 C  提供的单精确度 (single precision) 浮点数形
态. 在大多数的机器上会有七位数的精确度 (十进位).

翻译:	Spock @ FF	97.Aug.10.
