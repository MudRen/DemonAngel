名称：
	swap - 直接把一个档案交换 (swap) 出去〈译按：扔到硬碟的交换档里头〉。
语法：
	void swap( object );

	无传回值 swap( 物件 );
用法：
	本函式仅在编译驱动程式时使用 -DDEBUG 参数方能生效。
	
	这个外部函式应该只保留在除错用途。swap() 让一个物件直接被交换出去。
	如果要使用 swap()，建议最好使用 simul_efun 越权	(override) 处理 
	swap() 以防止滥用。

	注意：如果物件已经被摧毁了、已经被交换出去、有心跳、
	、被其他物件复制、被其他物件继承、可互动，则无法交换出去。
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻译：
	spock@muds.net		2000.Aug.20.	v22 版
