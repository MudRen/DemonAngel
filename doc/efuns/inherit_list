名称：
	inherit_list - 取得一个物件所继承的父物件 (parents) 清单。
语法：
	string array inherit_list( object obj );

	字串 阵列 inherit_list( 物件 物件 );
用法：
	传回物件“obj”所继承的父物件档案名称，以字串阵列的形式传回。
	如果编译时定义 COMPAT_32，本函式功能如同 deep_inherit_list()，不然
	它等于 shallow_inherit_list()。
参考：
	deep_inherit_list, shallow_inherit_list, inherits
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻译：
	spock@muds.net		2000.Aug.28.	v22 版
