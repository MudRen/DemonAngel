名称：
	all_previous_objects - 传回呼叫目前函式所有的物件阵列。
语法：
	object array all_previous_objects();
	
	物件 阵列 all_previous_objects();
用法：
	传回一个呼叫目前函式所有的物件阵列。 
	注意，区域函式呼叫不会把 previous_object() 设定为目前的物件，它不会
	变。
	
	阵列的第一个元素是 previous_object()，接着是 previous_object(1)，以
	此类推。
参考：
	call_other, origin, previous_object
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻译：
	spock@muds.net		2000.Aug.28.	v22 版
