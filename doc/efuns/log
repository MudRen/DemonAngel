名称：
	log() - 传回一个浮点数的自然对数 (natural logarithm)。
语法：
	float log( float f );

	浮点数 log( 浮点数 f );
用法：
	本函式仅在定义 PACKAGE_MATH 时生效。

	传回参数 f 的自然对数，f 必须为正值。
参数：
	exp, pow, sqrt
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻译：
	spock@muds.net		2000.May.28.	v22 版
