名称：
	m_indices - 传回一个映射的所有关键字 (key)。
语法：
	array m_indices( mapping );

	阵列 m_indices( 映射 );
用法：
	本外部函式仅在定义 COMPAT_32 时有效。它的功用与 keys() 相同。
参考：
	keys
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻译：
	spock@muds.net		2000.Aug.23.	v22 版
