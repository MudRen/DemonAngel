名称：
	in_edit - 判断一个玩家是否在编辑程式当中。
语法：
	string in_edit();
	string in_edit( object ob );

	字串 in_edit();
	字串 in_edit( 物件 物件 );
用法：
	如果一个指定的物件正在编辑程式中，会传回正在编辑的档案名称。如果不
	是则传回 0。如果不指定物件，就当作是 this_object()。
参考：
	ed, in_input
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻译：
	spock@muds.net		2000.Jun.25.	v22 版
