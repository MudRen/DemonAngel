名称：
	opcprof - 报告一些外部函式 (efun) 被呼叫的频率统计。
语法：
	void opcprof();
	void opcprof( string );

	无传回值 opcprof( );
	无传回值 opcprof( 字串 );
用法：
	这个函式印出每一个外部函式 (efunction) 和外部函式操作子	(eoperator) 
	的统计数字列表。如果不指定参数，所有的资料都会倾印到 /OPCPROF.efun 
	和 /OPCPROF.eoper 这两个档案里。如果指定了一个参数，就用作倾印的档
	案名称。
参考：
	function_profile
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻译：
	spock@muds.net		2000.Aug.19.	v22 版
