名称：
	find_living - 找出一个指定 id 〈识别名称〉的活 (living) 物件。
语法：
	object find_living( string str );

	物件 find_living( 字串 字串 );
用法：
	找第一个标记为活的 (living)，并符合 id 为“str”的物件。已经呼叫过 
	enable_commands() 的物件才视为活物件。并且此物件必须以 
	set_living_name() 设定名称，此名称才会放进搜寻表 (hash table) 中以
	提升寻找活物件的速度。
参考：
	living, livings, users, disable_commands, enable_commands, 
	set_living_name
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻译：
	spock@muds.net		2000.Aug.23.	v22 版
