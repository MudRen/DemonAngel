名称：
	keys - 传回一个阵列，是映射 (key, value)〈 即 (关键字, 内容值)〉资
	       料中全部的 key〈关键字〉。
语法：
	array keys( mapping m );

	阵列 keys( 映射 m );
用法：
	keys() 传回储存在映射 m 的 (关键字, 内容值) 资料之所有关键字〈索引
	〉的阵列。

	范例：

	mapping m;
	m = (["hp" : 35, "sp" : 42, "mass" : 100]);

	则:

	keys(m) == ({"hp", "sp", "mass"})

	请注意：关键字之间不会有任何顺序。不过其顺序与 values() 传回内容值
		所对应的顺序相同。
参考：
	values
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻译：
	spock@muds.net		2000.Aug.20.	v22 版
