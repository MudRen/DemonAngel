
函数使用解说(fun_help)：/adm/simul_efun/ 内函数解说


string to_chinese_kingdom(string id)	显示国家中文
string full_kingdom(string id)		完整的国家名称(含英文)

string user_data(string name)		使用者资料
string login_data(string name)		login资料
string mail_data(string name)		邮件资料

int check_kingdom(string id)		检查国家是否存在，存在就回应 1
int do_saveroom(object env)		房间存档
varargs mixed kingdom_data(string id, string type, string prop, string set, mixed data)
					国家资讯的处理
varargs mixed people_data(string id, string prop, string tar)
					人民的资料处理

string inverse_dir(string dir)		输入 dir 方向，回应 反相 dir
string kill_all_bug(mixed str)		消除可能存在的 bug，str 为字串
string ansi_color(string str)		上色的转换
int refresh_room(object env)		更新房间
varargs int check_env_prop(object env, string par, int flag, string type)
					检查环境的性质
					type : normal, temp, 内定 normal
					flag : 判定是否 normal, temp 一起都判定一次
						flag = 1 表“或”
						flag = 2 表“且”
void set_channel(object me, string channel, int flag)
					增加/减少频道
varargs void set_j_b_kingdom(object me, int flag, string id)
					加入/开除国籍
varargs void set_arrest(object me, int flag, string id)
					通缉与解通缉


