名称：
	filter_array() - 传回一个筛选过的次阵列 (sub-array)。
语法：
	array filter_array( array arr, string fun, object ob, mixed extra,
		 ... );
	array filter_array( array arr, function f, mixed extra, ... );

	阵列 filter_array( 阵列 阵列, 字串 函式, 物件 物件, 混合 其他,
		 ... );
	阵列 filter_array( 阵列 阵列, 函式指标 f, 混合 其他, ... );
用法：
	filter_array() 实际上就等于 filter() 外部函式。 
参考：
	filter
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻译：
	spock@muds.net		2000.May.21.	v22 版
