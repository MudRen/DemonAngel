名称：
	sizeof - 传回一个阵列的元素数目。
语法：
	int sizeof( mixed var );

	整数 sizeof( 混合 变数 );
用法：
	传回 var 中元素的数目, var 可以是阵列、映射、字串、暂存区。如果 var 
	不属于以上四种型态，就传回 0。
参考：
	strlen
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻译：
	spock@muds.net		2000.Jun.24.	v22 版
