名称：
	store_variable - 设定一个变数的值。
语法：
	void store_variable( string, mixed );
	
	无传回值 store_variable( 字串, 混合 );
用法：
	第一个参数是指定的变数，第二个参数是设定该变数的值。注意，变数必须
	先在 this_object() 中定义。

	本函式需要在 optionms.h 中定义 PACKAGE_CONTRIB。
参考：
	restore_variable, fetch_variable, save_object

作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻译：
	spock@muds.net		2000.Jun.24.	v22 版
