名称：
	preload - 预先载入一个物件到记忆体中。
语法：
	void preload( string filename );

	无传回值 preload( 字串 档名 );
用法：
	epilog 传回的阵列中，每一个字串都会让驱动程式呼叫 preload(filename)
	。请注意，在驱动程式层次作这些呼叫时，会有相等的 catch()，所以
	mudlib 不用担心这些程序因一个错误而停止。
参考：
	epilog, load_object
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻译：
	spock@muds.net		2000.May.21.	v22 版
