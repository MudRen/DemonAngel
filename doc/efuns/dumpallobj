名称：
	dumpallobj - 报告所有已载入之物件的一些统计资料。
语法：
	void dumpallobj();
	void dumpallobj( string );

	无传回值 dumpallobj();
	无传回值 dumpallobj( 字串 );
用法：
	这个函数会倾印 (dump) 出所有已经载入之物件的统计资料列表。如果不指
	定参数，这些资讯会倾印在 /OBJ_DUMP 这个档案里。如果指定参数，就是指
	定输出的档案名称。
参考：
	mud_status, debug_info
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻译：
	spock@muds.net		2000.Aug.19.	v22 版
