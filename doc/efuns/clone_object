名称：
	clone_object - 载入一个物件的复制品。
语法：
	object clone_object( string name, ... );
	object new( string name, ... );

	物件 clone_object( 字串 名称, ... );
	物件 new( 字串 名称, ... );
用法：
	从档案“name”创造一个新物件，并给予它一个独有的名称〈在名称后面加
	上“#xxx”，xxx 是编号〉。本函式传回新物件。新的物件与物件“name”
	共享相同的程式，但是它有自己的一套变数。第二个以后的参数会传入 
	create()。
参考：
	destruct, move_object, new
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻译：
	spock@muds.net		2000.Aug.26.	v22 版
