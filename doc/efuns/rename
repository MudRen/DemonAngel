名称：
	rename() - 更改一个档案的名称。
语法：
	int rename( string src, string dst );

	整数 rename( 字串 来源, 字串 目标 );
用法：
	把叫做 src 的档案改名叫做 dst。

	rename() 改名成功传回 0。失败则传回非零值。
参考：
	rm, rmdir, cp, link
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻译：
	spock@muds.net		2000.May.28.	v22 版
