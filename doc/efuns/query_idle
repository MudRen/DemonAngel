名称：
	query_idle - 询问一个互动 (interactive) 玩家已经发呆 (idle) 几秒。
语法：
	int query_idle( object ob );

	整数 query_idle( 物件 物件 );
用法：
	询问一个互动 (interactive) 玩家物件已经发呆 (idle) 几秒。
参考：
	in_edit, in_input
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻译：
	spock@muds.net		2000.Jul.1.	v22 版
