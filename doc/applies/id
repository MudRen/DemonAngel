名称：
	id - 此函式被 present() 呼叫，以鉴定一个物件的名称。
语法：
	int id( string an_id );

	整数 id( 字串 一个名称 );
用法：
	present() 外部函式 (efunction) 呼叫 id() 以判断一个指定的物件是否
	以字串 an_id 为名称。如果此物件可以此为名，就传回 1，不然则传回 0。
参考：
	present
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻译：
	spock@muds.net		2000.Apr.26.	v22 版
