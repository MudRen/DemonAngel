* 函式的原型 (prototype):

LPC 得函式原型与 ANSI C 非常相似. 函式原型可以做较好的型态检查, 并当作一
种“前宣告” ('forward' declaration).

传回值形态 函式名称( 第一个参数型态 参数一, 第二个参数形态 参数二, ... );

上面的函式在函式原型中, 参数不需要给名称:

传回值形态 函式名称( 参数一, 参数二, ... );

翻译:	Spock @ FF	97.Aug.10.
