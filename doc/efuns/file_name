名称：
	file_name - 取得一个物件的档案名称。
语法：
	string file_name( object ob );

	字串 file_name( 物件 ob );
用法：
	file_name() 传回载入“ob”时的档案名称。如果物件是复制物件，则 
	file_name() 传回的名称就不是磁碟中实际上所用的档案名称，而是在原来
	的档案名称之后加上“#”号，再加上物件的复制编号。当游戏启动时，物件
	的复制编号从 0 开始，每复制一个相同的物件就增加一号。所以每一个复制
	出来的物件都有自己独有的编号。如果不指定“ob”，则预设为 
	this_object()。
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻译：
	spock@muds.net		2000.Aug.26.	v22 版
