名称：
	floor() - 找到最靠近一个浮点数整数，小于或等于此浮点数。意即把一个浮点数的小数
		  位舍去.
语法：
	float floor( float f );

	浮点数 floor( 浮点数 f );
用法：
	本函式仅在定义 PACKAGE_MATH 时生效。

	传回一个最靠近浮点数 f，而等于或小于 f 的整数。
译注：
	意即把浮点数 f 的小数位舍去。
参考：
	ceil, to_int, to_float
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻译：
	spock@muds.net		2000.May.28.	v22 版
