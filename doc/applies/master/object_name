名称：
	object_name - 驱动程式呼叫此函式以得知一个物件的名称。
语法：
	string object_name( object );

	字串 object_name( 物件 );
用法：
	当 sprintf 函式要印出一个物件的“值”时，会呼叫此主宰物件应用函式
	(master apply)。此函式应传回一个字串，对应于该物件的名称〈例如一个
	使用者的名称〉。
参考：
	file_name
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻译：
	spock@muds.net		2000.May.21.	v22 版
